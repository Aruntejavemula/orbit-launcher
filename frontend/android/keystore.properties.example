# Copy to keystore.properties (gitignored) and fill in real values.
# Generate keystore: keytool -genkey -v -keystore remio-upload.jks -alias remio -keyalg RSA -keysize 2048 -validity 10000
storeFile=/absolute/path/to/remio-upload.jks
storePassword=CHANGE_ME
keyAlias=remio
keyPassword=CHANGE_ME
